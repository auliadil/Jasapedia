<?xml version="1.0" encoding="utf-8"?>
<font-family xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- regular -->
    <font
        android:font="@font/muli_regular"
        android:fontStyle="normal"
        android:fontWeight="400"

        app:font="@font/muli_regular"
        app:fontStyle="normal"
        app:fontWeight="400" />

    <!-- light -->
    <font
        android:font="@font/muli_light"
        android:fontStyle="normal"
        android:fontWeight="200"

        app:font="@font/muli_light"
        app:fontStyle="normal"
        app:fontWeight="200" />

    <!-- italic -->
    <font
        android:font="@font/muli_italic"
        android:fontStyle="italic"
        android:fontWeight="400"

        app:font="@font/muli_italic"
        app:fontStyle="italic"
        app:fontWeight="400" />

    <!-- semibold -->
    <font
        android:font="@font/muli_semibold"
        android:fontStyle="normal"
        android:fontWeight="500"

        app:font="@font/muli_semibold"
        app:fontStyle="normal"
        app:fontWeight="500" />

    <!-- bold -->
    <font
        android:font="@font/muli_bold"
        android:fontStyle="normal"
        android:fontWeight="700"

        app:font="@font/muli_bold"
        app:fontStyle="normal"
        app:fontWeight="700" />

    <!-- black -->
    <font
        android:font="@font/muli_black"
        android:fontStyle="normal"
        android:fontWeight="1000"

        app:font="@font/muli_black"
        app:fontStyle="normal"
        app:fontWeight="1000" />

</font-family>